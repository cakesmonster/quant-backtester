# 日晷 (Sundial) — 实施计划

> A股市场望远镜 · 观日影之移，知战机之瞬

---

## 一、项目定位

**日晷 = 市场数据看板**，专注于公开市场数据的聚合展示，不包含任何买卖策略逻辑。

- 已有能力来自：同花顺 API、mootdx 通达信 Level-2、东方财富 API
- 数据全部公开，无策略泄露风险
- 独立部署，不依赖 board_monitor / quant-backtester

---

## 二、技术选型

| 层 | 选型 | 说明 |
|----|------|------|
| Web 框架 | FastAPI | 与现有系统一致 |
| 前端 | 单页 HTML + Plotly.js + htmx | 零前端构建 |
| 个股行情 | mootdx (Level-2/逐笔/分钟K) + 腾讯财经 `qt.gtimg.cn` | 免费 |
| 板块行情 | 东方财富 push2 公开 API | 免费，无需 Key |
| 热榜 | 同花顺 `eq.10jqka.com.cn` | 免费 |
| 股票基础信息 | 东方财富 `push2.eastmoney.com/api/qt/stock/get` | 免费 |
| 分时图 | 东方财富 `push2/.../trends2/get` | 免费 |
| 时序存储 | Parquet | 日线K线本地缓存 |
| 元数据存储 | SQLite | 涨停/热榜/情绪/竞价/账户等结构化快照 |
| 部署 | systemd + 公网开关 | 同现有模式 |

### 东方财富 push2 公开 API

无需 API Key，直接 HTTP GET。

**行业板块行情（按涨跌幅排序）：**
```
GET https://push2.eastmoney.com/api/qt/clist/get
  ?pn=1&pz=50&po=1&np=1&fltt=2&invt=2&fid=f3
  &fs=m:90+t:2
  &fields=f2,f3,f4,f12,f14
```
- `fs=m:90+t:2` = 行业板块，`t:3` = 概念板块
- `fid=f3` = 按涨跌幅排序（`f3`=涨跌幅, `f2`=最新价, `f4`=涨跌额, `f12`=板块代码, `f14`=板块名称）
- 单个板块的成分股：`fs=b:BKxxxx+f:!50` 过滤指定板块代码

**个股基础信息：**
```
GET https://push2.eastmoney.com/api/qt/stock/get
  ?secid=1.600519
  &fields=f57,f58,f100,f116,f117,f162,f167,f168
```
- `secid` 格式：`1.` 前缀=沪市, `0.`=深市，后面跟 6 位代码
- `f57`=代码, `f58`=名称, `f100`=行业, `f116`=总市值, `f117`=流通市值, `f162`=市盈率, `f167`=换手率, `f168`=量比

### 数据源可靠性验证（2026-05-25 周日）

| 接口 | 测试结果 | 说明 |
|------|----------|------|
| 腾讯财经 `qt.gtimg.cn` | ✅ 可用 | 指数行情+成交额始终可用，无时段限制 |
| 东方财富 `clist`（全市场列表）| ⚠️ 周日无响应 | 疑似交易日才响应，需周一验证 |
| 东方财富 `stock/get`（个股信息）| ⚠️ 周日无响应 | 同上 |
| 东方财富 `trends2`（分时）| ⚠️ 不稳定 | 首次可用，连续请求后限流 |
| mootdx（通达信 Level-2）| ✅ 可用 | 不依赖 HTTP API，TCP 直连，始终在线 |

**涨停/跌停数据采集风险：** 当前方案依赖东方财富 `clist` 接口批量获取全市场涨跌情况，再通过 `f3`（涨跌幅 ≥ 9.8% ≈ 涨停）推算涨停股列表。如果交易日 `clist` 也不可用或有限流，备选方案：

1. **方案 A（主）**：`clist` 正常 → 批量拉取，通过涨跌幅判定涨停/跌停
2. **方案 B（备）**：mootdx 逐只拉取全市场日线 `close`/`pre_close` → 计算 `change_pct` → 筛选涨停股。问题：3300+ 只全量拉取较重，建议缓存+增量更新
3. **方案 C（兜底）**：同花顺热榜 API 自带涨停标记（仅热榜前 50），覆盖不全但零成本

**分时数据：**
```
GET https://push2.eastmoney.com/api/qt/stock/trends2/get
  ?secid=1.600519&ndays=1&iscr=1
  &fields1=f1,f2,f3,f4,f5,f6,f7,f8,f9,f10,f11,f12,f13
  &fields2=f51,f52,f53,f54,f55,f56,f57,f58
```
- 返回当日 1 分钟分时数据（约 240 条），字段：时间/开盘/现价/最高/最低/成交量/成交额/均价

---

## 三、页面结构（5 页）

```
┌─────────────────────────────────────────────────────────┐
│  日晷 Sundial                    [复盘] [热榜] [个股]    │
│                                  [回测] [账户]          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   页面1: 每日复盘    页面2: 热榜      页面3: 个股       │
│   页面4: 策略回测    页面5: 模拟账户                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 页面 1：每日复盘

**可选择日期回溯。** 所有数据从 SQLite 读取。

```
┌─────────────────────────────────────────────────────────┐
│ 📅 [选择日期 ▼]                                         │
├─────────────────────────────────────────────────────────┤
│  上证 +0.8%  深证 +1.2%  创业板 +2.1%  科创50 -0.3%    │
│  成交额 8,562亿  （较昨日 +12.3% 放量）                 │
├──────────┬──────────┬──────────┬──────────┬─────────────┤
│ 涨停 32  │ 跌停 5   │ 炸板 8   │ 炸板率20%│ 晋级率 45%  │
│ (昨28↑) │ (昨3↑)  │ (昨10↓) │ (昨26%↓)│ (昨50%↓)   │
├──────────┴──────────┴──────────┴──────────┴─────────────┤
│  🔥 情绪温度计: ████████████░░░░░░░░  68°C  偏热        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────┐                               │
│  │ 昨日涨停今日表现      │                               │
│  │ +2.3%  涨停5 红盘12  │                               │
│  │ 绿盘8  跌停3         │                               │
│  └──────────────────────┘                               │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 连板天梯                                          │ │
│  │     ...[同上]...                                   │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 板块攻击路线（资金流向）                            │ │
│  │     ...[同上]...                                   │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 情绪仪表盘详情

**指数行情：** 上证/深证/创业板/科创50 涨跌幅，数据来源腾讯财经 `qt.gtimg.cn`。

**成交额：** 沪深两市合计，对比昨日算出放量/缩量百分比。

**涨停/跌停：** 从 `limit_up_history` 统计当日总数，对比昨日标注 ↑↓。

**情绪温度计：** 综合加权计算（0°-100°）：
| 指标 | 权重 | 说明 |
|------|------|------|
| 涨停家数 | 30% | 越多越热 |
| 炸板率 | 20% | 越低越好（封板质量） |
| 连板晋级率 | 20% | 越高越强 |
| 指数涨跌 | 15% | 大盘方向 |
| 成交额变化 | 15% | 放量=活跃 |

温度区间：0-30° 冰点 / 30-50° 偏冷 / 50-70° 正常 / 70-85° 偏热 / 85-100° 过热

**竞价异动：** P2 功能，暂时隐藏不做。

---

### 页面 2：热榜

**可选择日期和时段（11:30 / 15:00 / 21:00）。**

```
┌─────────────────────────────────────────────────────────┐
│ 📅 [选择日期 ▼]  ⏰ [1130 ▼] [1500 ▼] [2100 ▼]        │
├─────────────────────────────────────────────────────────┤
│ 排名  │ 代码    │ 名称    │ 热度  │ 概念    │ 涨跌   │
│ 1     │ 600xxx │ 某某股份 │ 98500 │ 芯片    │ +10.0% │
│ 2     │ 000xxx │ 某某科技 │ 87200 │ 新能源  │ +9.98% │
│ 3     │ ...    │ ...     │ ...   │ ...     │ ...    │
│                                                         │
│ [hover 任意股票 → 基础信息卡片]                          │
│                                                         │
│ [点击涨停股 → 跳转页面3 个股分析]                        │
│ [点击涨停股 → 弹出找队友推荐]                            │
└─────────────────────────────────────────────────────────┘
```

---

### 页面 3：个股分析

**只看当天，不选日期。** 输入股票代码后实时拉取。

```
┌─────────────────────────────────────────────────────────┐
│ 🔍 [输入代码: 600xxx] [查询]                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────┐  ┌──────────────────────────┐ │
│  │ 600xxx 某某股份       │  │ 👥 找队友                │ │
│  │ 市值: 580亿          │  │                          │ │
│  │ PE: 45.2             │  │ 同概念:                  │ │
│  │ 换手率: 18.7%        │  │   601aaa  芯片龙头       │ │
│  │ 量比: 2.1            │  │   002bbb  存储芯片       │ │
│  ├──────────────────────┤  │                          │ │
│  │ 📈 今日分时           │  │ 走势相似:               │ │
│  │ [Plotly 1-min K线图] │  │   003ccc  r=0.87  +5.2% │ │
│  └──────────────────────┘  │   004ddd  r=0.82  +3.1% │ │
│                            │   005eee  r=0.79  +1.8% │ │
│  ┌──────────────────────┐  └──────────────────────────┘ │
│  │ 大单异动              │                              │
│  │ 09:35  买入  5000手  │                              │
│  │ 10:12  卖出  3000手  │                              │
│  │ 13:05  买入  8000手  │                              │
│  └──────────────────────┘                              │
└─────────────────────────────────────────────────────────┘
```

- 分时图：mootdx 1-min K 线实时拉取
- 大单：mootdx 逐笔成交实时拉取
- 找队友：同概念从热榜 API、走势相似度从分钟 K 线 Pearson 计算

---

### 页面 4：策略回测

**嵌入 quant-backtester 的现有功能。** 可选择策略 + 参数，跑完看收益曲线和指标。

与 quant-backtester 的关系：API 透传或嵌入 iframe。不做二次开发，直接复用现有能力。

---

### 页面 5：模拟账户

**可选择日期回溯。** 每日收盘后拍快照存 SQLite。

```
┌─────────────────────────────────────────────────────────┐
│ 📅 [选择日期 ▼]                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────┐ │
│  │ 总资产    │ │ 可用资金  │ │ 持仓市值  │ │ 今日盈亏   │ │
│  │ 215,000   │ │ 45,000   │ │ 170,000  │ │ +3,200    │ │
│  └──────────┘ └──────────┘ └──────────┘ └───────────┘ │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 📈 收益趋势图（Plotly 曲线）                        │ │
│  │    [每日快照累积的权益曲线]                         │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 当前持仓                                           │ │
│  │ 代码    名称    数量   成本   现价   盈亏%          │ │
│  │ 600xxx  xx股份  5000   8.50   9.20   +8.2%         │ │
│  │ 000xxx  xx科技  3000  12.30  11.80   -4.1%         │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 今日成交                                           │ │
│  │ 时间    代码    方向    价格    数量    金额        │ │
│  │ 09:35   600xxx  买入    9.20    1000    9200       │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

收益趋势图：每天收盘后自动拍快照（总资产、持仓市值），攒起来就是收益曲线。

---

## 四、数据存储设计

### SQLite（4 张表）

```sql
-- 涨停历史（每日增量）
CREATE TABLE limit_up_history (
    date TEXT, code TEXT, name TEXT,
    board_count INTEGER,        -- 连板数
    first_time TEXT,            -- 首次触板时间
    last_time TEXT,             -- 最后封板时间
    is_broken BOOLEAN,          -- 是否炸板
    sector TEXT,                -- 所属板块
    turnover REAL,              -- 换手率
    volume_ratio REAL,          -- 量比
    PRIMARY KEY (date, code)
);

-- 热榜快照（三时段：1130 / 1500 / 2100）
CREATE TABLE hot_rank_snapshot (
    date TEXT, slot TEXT,
    rank INTEGER, code TEXT, name TEXT,
    heat_value REAL,
    concept_tag TEXT,
    is_limit_up BOOLEAN,
    change_pct REAL,
    PRIMARY KEY (date, slot, code)
);

-- 每日情绪快照（页面1 仪表盘数据）
CREATE TABLE daily_sentiment (
    date TEXT PRIMARY KEY,
    -- 指数
    sh_close REAL, sh_change_pct REAL,
    sz_close REAL, sz_change_pct REAL,
    cyb_close REAL, cyb_change_pct REAL,     -- 创业板指 399006
    kcb_close REAL, kcb_change_pct REAL,    -- 科创50 000688
    -- 成交额
    total_amount REAL,                       -- 沪深合计（亿）
    amount_change_pct REAL,                  -- 对比昨日变化%
    -- 涨停/跌停
    limit_up_count INTEGER,
    limit_down_count INTEGER,
    broken_count INTEGER,
    broken_rate REAL,
    -- 晋级
    promotion_rate REAL,
    -- 昨日涨停表现
    yday_avg_return REAL,
    yday_cont_up INTEGER,
    yday_red INTEGER,
    yday_green INTEGER,
    yday_limit_down INTEGER,
    -- 情绪温度计
    sentiment_temp REAL                      -- 0-100
);

-- 模拟账户每日快照
CREATE TABLE account_snapshot (
    date TEXT PRIMARY KEY,
    total_asset REAL,
    available_cash REAL,
    position_value REAL,
    daily_pnl REAL,
    holdings TEXT               -- JSON: [{code, name, qty, cost, price, pnl_pct}]
);

-- 竞价异动（每日一条 JSON）
CREATE TABLE auction_alert (
    date TEXT PRIMARY KEY,
    alerts TEXT                 -- JSON: [{time, code, name, type, detail}]
);
```

### 不存储（实时拉取）

| 数据 | 来源 |
|------|------|
| 股票基础信息（市值/PE/概念） | 东方财富 API + 同花顺概念 |
| 1-min 分时 K 线 | mootdx `get_bars(frequency=7)` |
| 大单逐笔成交 | mootdx `get_transaction_data()` |
| 板块涨跌幅排行 | 东方财富板块 API |
| 模拟账户实时持仓 | 同花顺模拟交易 API |

### Parquet（时序）

日线 K 线沿用 quant-backtester 的 Parquet 缓存模式：`data/cache/{code}.parquet`，增量更新。

---

## 五、项目结构

```
sundial/
├── pyproject.toml
├── README.md
├── docs/
│   └── PLAN.md
├── src/sundial/
│   ├── main.py                    # FastAPI 入口 :8200
│   ├── config.py                  # 配置 + .env 加载
│   ├── data/
│   │   ├── fetcher.py             # mootdx K线/逐笔/分时
│   │   ├── ths_api.py             # 同花顺 API（热榜/概念）
│   │   ├── eastmoney_api.py       # 东方财富 API（板块/基础信息）
│   │   ├── trade_api.py           # 同花顺模拟交易 API
│   │   └── db.py                  # SQLite 读写
│   ├── services/
│   │   ├── ladder.py              # 连板天梯计算
│   │   ├── sentiment.py           # 情绪指标
│   │   ├── hot_rank.py            # 热榜采集 + 快照
│   │   ├── teammate.py            # 找队友（相关性计算）
│   │   ├── big_order.py           # 大单筛选
│   │   ├── auction.py             # 竞价异动
│   │   └── sector_flow.py         # 板块资金流向
│   └── dashboard/
│       └── templates/
│           ├── review.html         # 页面1: 每日复盘
│           ├── hotrank.html        # 页面2: 热榜
│           ├── stock.html          # 页面3: 个股分析
│           ├── backtest.html       # 页面4: 策略回测
│           └── account.html        # 页面5: 模拟账户
├── tests/
├── scripts/
│   ├── toggle-public.sh
│   └── daily_snapshot.sh           # 盘后快照 cron（15:30）
└── deploy/
    └── sundial.service
```

---

## 六、实施顺序

### Phase 1: 骨架 + 数据层
1. 项目初始化（pyproject.toml + FastAPI + SQLite）
2. ⚠️ **数据源交易日验证**（周一确认：clist 全市场、stock/get、trends2 是否可用；如不可用则切换到备选方案 B/C）
3. 数据源封装（mootdx / 同花顺 / 东方财富 / 腾讯财经）
4. SQLite 建表 + 涨停数据采集
5. 盘后快照脚本（cron 15:30）
6. systemd 部署 + 公网开关

### Phase 2: 页面1 每日复盘
6. 情绪仪表盘
7. 连板天梯 + hover 基础信息
8. 昨日涨停今日表现
9. 板块攻击路线
10. 竞价异动展示 + 采集

### Phase 3: 页面2+3 热榜+个股
11. 热榜页面（三时段查询 + 历史回溯）
12. 个股分析页面（分时图 + 大单 + 找队友）

### Phase 4: 页面4+5 回测+账户
13. 策略回测页面（嵌入 quant-backtester）
14. 模拟账户页面（持仓/收益曲线/成交记录）

---

## 七、端口分配

| 服务 | 端口 | 绑定 | 说明 |
|------|------|------|------|
| board_monitor | 8099 | 127.0.0.1 | 私有（打板策略） |
| quant-backtester | 8100 | 127.0.0.1 | 私有（回测引擎） |
| **日晷** | **8200** | toggle 开关 | **公开看板** |
